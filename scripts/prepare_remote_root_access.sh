#!/usr/bin/env bash
set -Eeuo pipefail

# This script is a placeholder. In production, you would:
#  - create an SSH keypair on the manager (if missing)
#  - copy the public key to /root/.ssh/authorized_keys on the remote
#  - enable PermitRootLogin prohibit-password (or yes) and PasswordAuthentication no
#  - restart sshd

echo "NOT IMPLEMENTED: prepare_remote_root_access.sh"
